.venv/
__pycache__/
*.pyc
.env

# Cloned upstream frameworks — not our code, re-clone per README instead
SWE-bench/
swe-bench-tasks/

# Regenerated experiment output — noisy on every re-run, and superseded by
# Weave (Calls trace + Agents conversation + blue-agent feedback) as the
# actual system of record.
runs/
logs/

# improve_agent.py's mutable state — a missing agent_config.json behaves like
# the empty/baseline config, and the round-by-round history it mutates through
# is also fully captured in Weave (the improve-loop-<instance_id> conversation).
agent_config.json
agent_config_history/
